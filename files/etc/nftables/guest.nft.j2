
{% if service_guest_enabled|default(false) == true %}
table inet guest {
  chain postrouting {
    type nat hook postrouting priority srcnat;
    meta iifname guest meta oifname != guest masquerade
  }
}
{% endif %}

