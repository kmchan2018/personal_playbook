{%- extends 'files/guest.instances.inc.j2' -%}{%- block body -%}
#!/bin/bash

if [[ $EUID -eq 0 ]]; then
  exec systemctl start {{ name }}.service
else
  exec sudo /usr/local/bin/{{ name }}-start "$@"
fi

{% endblock %}
