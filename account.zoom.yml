

- hosts: "all"
  become: true
  become_user: "kmchan"

  tasks:

    - name: "Applying Zoom Settings to Account"
      when: "account_zoom_enabled|default(false)"
      block:

        - name: "Applying Zoom Settings to Account: Copying Configuration Files"
          copy:
            dest: "/home/kmchan/{{ item.path }}"
            src: "{{ playbook_dir }}/files/home/kmchan/{{ item.path }}"
            owner: "kmchan"
            group: "kmchan"
            mode: "{{ item.mode }}"
          loop:
            - { path: ".local/share/applications/Zoom.desktop", mode: "0644" }


